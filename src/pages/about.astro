---
import { Image } from "astro:assets";
import Layout from "../layouts/Layout.astro";
import logo from "@/assets/ui/logo.png";
import { siteData, ourValues, employees, history } from "../data/siteData";
---

<Layout
  title={`Sobre Nosotros - ${siteData.name}`}
  description="Conoce nuestra misión, valores y barberos expertos"
>
  <section
    class="pt-32 pb-20 bg-linear-to-br from-slate-900 via-slate-800 to-slate-900"
  >
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center max-w-3xl mx-auto">
        <Image
          src={logo}
          alt={`Logo - ${siteData.name}`}
          class="block mx-auto w-40 h-40 lg:w-64 lg:h-64 object-contain mb-6"
          loading="lazy"
        />
        <h1 class="text-5xl md:text-6xl font-bold text-white mb-6">
          ¿Quiénes somos?
        </h1>
        <p class="text-xl text-gray-300 mb-6">
          En Imperio Design Barber Shop somos una barbería pensada para quienes
          valoran el buen estilo y un servicio de calidad. Combinamos técnicas
          clásicas y modernas para ofrecer cortes precisos, acabados impecables
          y un ambiente cómodo para todo tipo de clientes.
        </p>
        <p class="text-xl text-gray-300">
          Nuestro equipo trabaja con profesionalismo y pasión, cuidando cada
          detalle para que cada visita sea una experiencia agradable y salgas
          con confianza en tu imagen.
        </p>
      </div>
    </div>
  </section>

  <section class="py-20 bg-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="max-w-4xl mx-auto">
        <h2 class="text-4xl font-bold text-white mb-8 text-center">
          Nuestra Misión
        </h2>
        <div class="prose prose-invert max-w-none">
          <p class="text-xl text-gray-300 mb-6">
            En Imperio Design Barber Shop nuestra misión es ofrecer servicios de
            barbería de alta calidad, combinando técnicas clásicas y modernas
            para brindar cortes precisos, estilos actuales y una experiencia
            cómoda y profesional.
          </p>
          <p class="text-lg text-gray-300 mb-6">
            Trabajamos con pasión y compromiso, cuidando cada detalle para que
            cada cliente salga satisfecho y con confianza en su imagen.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Values Section -->
  <section class="py-20 bg-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-white mb-12 text-center">
        Nuestros Valores
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
        {
          ourValues.map((value) => (
            <div class="bg-slate-800 p-8 rounded-xl border border-slate-700 text-center hover:border-cyan-500 transition-all">
              <div class="w-20 h-20 bg-cyan-500/10 rounded-lg flex items-center justify-center mx-auto mb-6">
                <i class={`${value.icon} text-4xl text-cyan-500`} />
              </div>
              <h3 class="text-2xl font-bold text-white mb-3">{value.value}</h3>
              <p class="text-gray-400 leading-relaxed">{value.description}</p>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <section class="py-20 bg-slate-900">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-white mb-12 text-center">
        Nuestra Historia
      </h2>
      <div class="max-w-4xl mx-auto">
        <div class="relative">
          <div
            class="absolute left-8 top-0 bottom-0 w-0.5 bg-cyan-500/30 hidden md:block"
          >
          </div>

          <div class="space-y-12">
            {
              history.map((milestone, index) => (
                <div class="relative flex items-start gap-6">
                  <div class="hidden md:block shrink-0">
                    <div class="w-16 h-16 bg-cyan-500 rounded-full flex items-center justify-center border-4 border-slate-900">
                      <span class="text-white font-bold text-sm">
                        {milestone.year}
                      </span>
                    </div>
                  </div>
                  <div class="flex-1 bg-slate-800 p-6 rounded-xl border border-slate-700 hover:border-cyan-500 transition-all">
                    <div class="md:hidden mb-2">
                      <span class="bg-cyan-500 text-white px-3 py-1 rounded-full text-sm font-bold">
                        {milestone.year}
                      </span>
                    </div>
                    <h3 class="text-2xl font-bold text-white mb-2">
                      {milestone.title}
                    </h3>
                    <p class="text-gray-300 leading-relaxed">
                      {milestone.description}
                    </p>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="py-20 bg-slate-800">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 class="text-4xl font-bold text-white mb-4 text-center">
        Nuestro Equipo
      </h2>
      <p class="text-xl text-gray-300 mb-12 text-center max-w-2xl mx-auto">
        Conozca a nuestros profesionales certificados dedicados darte el mejor
        servicio.
      </p>
      <div class="grid grid-cols-1 md:grid-cols-4 lg:grid-cols-6 gap-8">
        {
          employees.map((employee, index) => {
            let className = "";
            if (index + 1 == 4) {
              className = "lg:col-start-2";
            } else if (index + 1 == 5) {
              className = "md:col-start-2 lg:col-start-4";
            }
            return (
              <div
                class={`bg-slate-900 p-6 rounded-xl border border-slate-700 hover:border-cyan-500 transition-all transform hover:scale-105 md:col-span-2 ${className}`}
              >
                <div class="text-center mb-3">
                  <div class="w-40 h-40 rounded-full mx-auto mb-4 overflow-hidden border-4 border-cyan-500/20">
                    <Image
                      src={employee.image}
                      alt={`${employee.name} - ${employee.role}`}
                      width={400}
                      height={400}
                      class="w-full h-full object-cover"
                      loading="lazy"
                    />
                  </div>
                  <h3 class="text-xl font-bold text-white mb-2">
                    {employee.name}
                  </h3>
                  <p class="text-cyan-500 mb-1 font-semibold">{employee.role}</p>
                  <p class="text-gray-500 text-xs">
                    {employee.experience} experience
                  </p>
                </div>
                <p class="text-gray-300 text-sm leading-relaxed mb-4 text-center">
                  {employee.bio}
                </p>
                <div class="flex flex-wrap gap-2 justify-center">
                  {employee.certifications.map((cert) => (
                    <span class="bg-slate-800 text-gray-300 text-xs px-3 py-1 rounded-full border border-slate-700">
                      {cert}
                    </span>
                  ))}
                </div>
              </div>
            );
          })
        }
      </div>
    </div>
  </section>
</Layout>
